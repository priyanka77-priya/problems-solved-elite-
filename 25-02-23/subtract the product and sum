class Solution {
    public int subtractProductAndSum(int n) {
        int pro=1,sum=0;
        while(n!=0){
           int right=n%10;
            n/=10;
            sum+=right;
            pro*=right;
           
        }
        return pro-sum;
        
    }
}
